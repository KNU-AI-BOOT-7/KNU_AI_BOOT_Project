# 백엔드 주소 재정의 (선택). 미지정 시 http://127.0.0.1:8000 사용.
# CLOVA 키는 더 이상 프론트에 두지 않습니다 — 백엔드(.env)에만 보관합니다.
#
# 실기기(Expo Go)에서 테스트하려면 PC의 LAN IP로 지정하세요.
#   EXPO_PUBLIC_API_BASE=http://192.168.0.10:8000
EXPO_PUBLIC_API_BASE=http://127.0.0.1:8000
